[project]
name = "transcript-downloader"
version = "0.1.0"
description = "Download YouTube interview transcripts into transcripts/<slug>-<YYYYMMDD>/"
requires-python = ">=3.12"
dependencies = [
    "youtube-transcript-api>=1.0",
    "yt-dlp>=2024.08.06",
]
